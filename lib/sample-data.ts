import { Invoice, ProductCatalogItem } from './types';

// Clean initial empty state - ready for custom products and invoices
export const INITIAL_PRODUCTS: ProductCatalogItem[] = [];

export const INITIAL_INVOICES: Invoice[] = [];
