rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // السماح بالقراءة والكتابة للمستخدمين المسجلين بحسابهم
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
